import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btnContainer:{ // estilo para el boton de agregar restaurante
        position: 'absolute',
        bottom:10,
        right:10
    },
    content:{
        flex:1,
    }
});